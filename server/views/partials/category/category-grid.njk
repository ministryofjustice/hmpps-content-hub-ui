{% set showSeriesTag =  showSeriesTag | default(true) %}
{% set showContentTag = showContentTag | default(true) %}
{% set showItemSummary = showItemSummary | default(false) %}

<div class="govuk-grid-row hmpps-category-grid">
	{% for item in gridItems %}
		<div class="govuk-grid-column-one-quarter">
			{% set cardItem = { href: item.contentUrl, title: item.title, thumbnailUrl: item.image.url if item.image else '', contentType: item.contentType } %}
			{% include "./category-card.njk" %}
			{% if item.summary and showItemSummary %}
				<p class="govuk-!-margin-top-2">{{ item.summary }}</p>
			{% endif %}
		</div>
	{% endfor %}
</div>
